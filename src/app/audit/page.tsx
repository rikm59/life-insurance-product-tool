import {AuditUploadPanel} from '@/components/AuditUploadPanel';

/**
 * Audit page provides functionality to upload and analyze carrier illustration
 * PDFs. A sample run is provided using seed data.
 */
export default function AuditPage() {
  return <AuditUploadPanel />;
}