import {ChatLayout} from '@/components/ChatLayout';

/**
 * Builder page loads the chat-based interface for constructing insurance
 * product tools.
 */
export default function BuilderPage() {
  return <ChatLayout />;
}